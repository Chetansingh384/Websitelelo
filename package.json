{
    "name": "websitelelo-root",
    "version": "1.0.0",
    "description": "Root package to manage Websitelelo project",
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix admin\"",
        "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend && npm install --prefix admin"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}